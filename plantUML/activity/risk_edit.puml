@startuml 
title 修改風險
start

:載入修改風險頁面;

:顯示風險清單與欄位
(ID、資產類別、資產名稱、威脅名稱、弱點名稱、風險值);

partition "使用者互動" {

    if (輸入搜尋關鍵字?) then (是)
        :過濾風險清單 (根據搜尋條件);
    endif
    fork
        if (點選「＋新增風險」?) then (是)
            :開啟新增風險表單;
            :填寫基本資料 (名稱、數值、描述);
            :選擇類型;
            :填寫審核資料;
            :上傳資料;
            :自動寫入建立時間;
            :送出表單;
        endif
    fork again
        if (點選「＋匯入風險」?) then (是)
            :上傳風險檔案;
            :解析並儲存資料;
            :填寫審核資料;
            :自動寫入建立時間;
            :送出表單;
        endif
    fork again
        if (編輯或刪除風險?) then (是)
            :顯示編輯/刪除操作介面;
            :執行操作;
            :寫入建立時間;
            :更新風險清單;
        endif
    end fork
}

stop

@enduml
